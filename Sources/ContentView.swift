import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("VitaMind")
            .font(.largeTitle)
            .foregroundColor(.primary)
            .padding()
    }
}

#Preview {
    ContentView()
}